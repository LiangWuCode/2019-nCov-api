server:
  port: 80
spring:
  application:
    name: ncov
  cloud:
    sentinel:
       transport:
          dashboard: 127.0.0.1:8088
  redis:
    port: 6379
    database: 0
    host: 127.0.0.1
    password:
    jedis:
      pool:
        max-active: 8
        max-wait: -1ms
        max-idle: 8
        min-idle: 0
    timeout: 5000ms

